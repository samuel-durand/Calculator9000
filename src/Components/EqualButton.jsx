function EqualButton(props) {
    const equal = "=";
  
    const handleClick = () => {

    };
  
    return (
      <h2 onClick={handleClick}>{equal}</h2>
    );
  }
  
  export default EqualButton;
  